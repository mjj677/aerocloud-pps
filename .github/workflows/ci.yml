name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET 10
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Restore dependencies
        run: dotnet restore AeroCloud.PPS.sln

      - name: Build
        run: dotnet build AeroCloud.PPS.sln --no-restore --configuration Release

      - name: Run tests
        run: dotnet test Tests/AeroCloud.PPS.Tests.csproj --no-build --configuration Release --verbosity normal
